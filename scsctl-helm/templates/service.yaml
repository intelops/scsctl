apiVersion: v1
kind: Service
metadata:
  name: {{ include "scsctl-helm.fullname" . }}
  labels:
    {{- include "scsctl-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scsctl-helm.selectorLabels" . | nindent 4 }}
