apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scsctl-helm.fullname" . }}-test-connection"
  labels:
    {{- include "scsctl-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scsctl-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
